# AI Maskotu — Proaktif Kategori Soruları Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ana ekranda bir tanı kategorisine tıklanınca, o kategoriyle ilgili bağlamsal bir soru soran, animasyonlu bir AI maskotu (mevcut BrandMark tabanlı) belirsin — hem ekranın köşesinde kısa bir "canlanma" olarak (variant A), hem kategori detay panelinin içine gömülü bir şerit olarak (variant B).

**Architecture:** Saf karar mantığı (`shouldShowMascot`/`computeFindingSignature`) UI'dan ayrı, test edilebilir bir modülde yaşar. Görsel bileşen (`AiMascotBubble`) mevcut `BrandMark` + Tailwind animasyon sözlüğünü kullanır, yeni bağımlılık yok. Soru metni önce sabit bir i18n sözlüğünden (anında, Ollama gerektirmez) gelir; Ollama çalışıyorsa (ve sohbet çekmecesi KAPALIYSA — stream event çakışmasını önlemek için) arka planda zenginleştirilir. Tıklanınca mevcut `AiChatDrawer`'a yeni bir `pendingMascotMessage` prop'uyla aktarılır.

**Tech Stack:** React 18 + TypeScript (frontend), Rust/Tauri (backend `settings.rs`'e küçük bir alan eklenir — yeni komut YOK), Vitest (`environment: "node"` — proje şu an jsdom/component-render testi ALTYAPISINA SAHİP DEĞİL, bkz. Task 1 notu).

**Spec:** `docs/superpowers/specs/2026-09-09-ai-mascot-design.md`

---

## Önemli bir düzeltme (spec'ten implementasyona geçerken bulundu)

Spec'in Test bölümü "`AiMascotBubble` için vitest" diyordu ama proje şu an
**yalnızca** `vitest.config.ts`'de `environment: "node"` ile saf `.ts`
fonksiyon testleri çalıştırıyor (`src/**/*.test.ts` — `.tsx` DEĞİL, jsdom/
React Testing Library YOK; bu proje genelinde bilinçli bir erteleme, tek
mevcut örnek: `src/lib/__tests__/toError.test.ts`). Bu yüzden:

- **Karar mantığı** (imza hesaplama, "tekrar göster mi") saf `.ts`
  fonksiyonlara çıkarılır → gerçek vitest testi alır (Task 1).
- **`AiMascotBubble.tsx` bileşeninin kendisi** projedeki diğer tüm UI
  bileşenleri gibi otomatik test ALMAZ — elle doğrulama (`npm run tauri dev`
  ile gerçek tıklama) yeterli, projenin geri kalanıyla tutarlı.

---

## Task 1: Bulgu imzası + "tekrar göster mi" karar mantığı

**Files:**
- Create: `src/lib/mascotSignature.ts`
- Test: `src/lib/__tests__/mascotSignature.test.ts`

- [ ] **Step 1: Write the failing test**

```typescript
// src/lib/__tests__/mascotSignature.test.ts
import { describe, expect, it } from "vitest";
import { computeFindingSignature, shouldShowMascot } from "../mascotSignature";
import type { Finding } from "../types";

function finding(id: string): Finding {
  return {
    id,
    category: "Disk",
    title: "",
    description: "",
    severity: "critical",
    metric: null,
    recommendedAction: null,
    action: null,
  };
}

describe("computeFindingSignature", () => {
  it("bulgu id'lerini sıralı birleştirir, sırayı görmezden gelir", () => {
    const a = computeFindingSignature([finding("b"), finding("a")]);
    const b = computeFindingSignature([finding("a"), finding("b")]);
    expect(a).toBe(b);
  });

  it("farklı bulgu kümesi için farklı imza üretir", () => {
    const a = computeFindingSignature([finding("a")]);
    const b = computeFindingSignature([finding("a"), finding("b")]);
    expect(a).not.toBe(b);
  });

  it("boş liste için boş string döner", () => {
    expect(computeFindingSignature([])).toBe("");
  });
});

describe("shouldShowMascot", () => {
  it("kategori daha önce hiç görülmediyse true döner", () => {
    expect(shouldShowMascot("disk-full", "sig-1", {})).toBe(true);
  });

  it("aynı imzayla ikinci kez false döner", () => {
    const seen = { "disk-full": "sig-1" };
    expect(shouldShowMascot("disk-full", "sig-1", seen)).toBe(false);
  });

  it("imza değişince tekrar true döner", () => {
    const seen = { "disk-full": "sig-1" };
    expect(shouldShowMascot("disk-full", "sig-2", seen)).toBe(true);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/lib/__tests__/mascotSignature.test.ts`
Expected: FAIL — `Cannot find module '../mascotSignature'`

- [ ] **Step 3: Write minimal implementation**

```typescript
// src/lib/mascotSignature.ts
import type { Finding } from "./types";

/** Bir bulgu kümesinin kararlı (sıra-bağımsız) imzası — aynı bulgular aynı
 * imzayı üretir, id'lerden biri değişirse/eklenirse/çıkarsa imza değişir. */
export function computeFindingSignature(findings: Finding[]): string {
  return findings
    .map((f) => f.id)
    .sort()
    .join("|");
}

/** Bu kategori için maskot kendiliğinden belirmeli mi? Yalnız imza daha önce
 * görülmemişse (veya değiştiyse) true. */
export function shouldShowMascot(
  categoryKey: string,
  signature: string,
  seenSignatures: Record<string, string>
): boolean {
  if (!signature) return false;
  return seenSignatures[categoryKey] !== signature;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run src/lib/__tests__/mascotSignature.test.ts`
Expected: PASS (5 tests)

- [ ] **Step 5: Commit**

```bash
git add src/lib/mascotSignature.ts src/lib/__tests__/mascotSignature.test.ts
git commit -m "feat(mascot): finding-signature + re-show decision logic"
```

---

## Task 2: Kategori başına sabit soru sözlüğü (TR/EN)

**Files:**
- Create: `src/lib/i18n/mascotPrompts.tr.ts`
- Create: `src/lib/i18n/mascotPrompts.en.ts`
- Test: `src/lib/i18n/__tests__/mascotPrompts.test.ts`

`categoryDefs.tsx`'teki 13 kategori key'i: `disk-full`, `disk-health`,
`chkdsk`, `events`, `drivers`, `virus`, `thermal`, `security`, `updates`,
`startup`, `crashes`, `pagefile`, `cleanup`.

- [ ] **Step 1: Write the failing test**

```typescript
// src/lib/i18n/__tests__/mascotPrompts.test.ts
import { describe, expect, it } from "vitest";
import { CATEGORIES } from "../../../components/categoryDefs";
import { mascotPromptsTr } from "../mascotPrompts.tr";
import { mascotPromptsEn } from "../mascotPrompts.en";

describe("mascotPrompts kapsama", () => {
  it("her CATEGORIES key'i için TR sorusu var", () => {
    for (const cat of CATEGORIES) {
      expect(mascotPromptsTr, `TR eksik: ${cat.key}`).toHaveProperty(cat.key);
      expect(mascotPromptsTr[cat.key as keyof typeof mascotPromptsTr].length).toBeGreaterThan(0);
    }
  });

  it("her CATEGORIES key'i için EN sorusu var", () => {
    for (const cat of CATEGORIES) {
      expect(mascotPromptsEn, `EN eksik: ${cat.key}`).toHaveProperty(cat.key);
    }
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/lib/i18n/__tests__/mascotPrompts.test.ts`
Expected: FAIL — `Cannot find module '../mascotPrompts.tr'`

- [ ] **Step 3: Write minimal implementation**

```typescript
// src/lib/i18n/mascotPrompts.tr.ts
// Kategori → maskotun kendiliğinden soracağı sabit soru. Ollama kurulu
// olmasa da anında görünür (bkz. docs/superpowers/specs/2026-09-09-ai-mascot-design.md).
export const mascotPromptsTr = {
  "disk-full": "Diskin dolmaya başlamış — hangi klasörler yer kaplıyor, birlikte bakalım mı?",
  "disk-health": "Disklerinden birinde sağlık uyarısı var — ne anlama geldiğini anlatayım mı?",
  chkdsk: "Son 30 günde disk/NTFS hatası kaydedilmiş. chkdsk taramasının ne yapacağını merak ediyor musun?",
  events: "Olay günlüğünde tekrar eden bir hata var — bunun sistemine etkisini konuşalım mı?",
  drivers: "Bazı sürücülerin imzasız veya eski — hangilerinin öncelikli olduğunu birlikte bulalım mı?",
  virus: "Virüs/Defender tarafında dikkat gereken bir şey var — ne yapman gerektiğini anlatayım mı?",
  thermal: "Sistemin ısınıyor veya performans düşüyor gibi — nedenini birlikte araştıralım mı?",
  security: "Güvenlik ayarlarında (güvenlik duvarı/UAC gibi) bir boşluk var — neden önemli, anlatayım mı?",
  updates: "Bekleyen güncellemelerin var — hangilerinin öncelikli olduğunu konuşalım mı?",
  startup: "Açılışın yavaşlamış olabilir — hangi programların yavaşlattığını birlikte bulalım mı?",
  crashes: "Son zamanlarda tekrar eden bir çökme/donma var — sebebini merak ediyor musun?",
  pagefile: "Sanal bellek ayarın gözden geçirilebilir — neden önerdiğimi anlatayım mı?",
  cleanup: "Temizlenebilecek epey yer var — nereden başlamak istersin?",
} as const;

export type MascotCategoryKey = keyof typeof mascotPromptsTr;
```

```typescript
// src/lib/i18n/mascotPrompts.en.ts
// Typed against `MascotCategoryKey` so a missing key is a compile error
// (same pattern as findings.en.ts / FindingCode).
import type { MascotCategoryKey } from "./mascotPrompts.tr";

export const mascotPromptsEn: Record<MascotCategoryKey, string> = {
  "disk-full": "Your disk is filling up — want to look at what's taking up space together?",
  "disk-health": "One of your disks has a health warning — want me to explain what it means?",
  chkdsk: "A disk/NTFS error was logged in the last 30 days. Curious what a chkdsk scan would do?",
  events: "A recurring error is showing up in the event log — want to talk through what it means?",
  drivers: "Some drivers are unsigned or old — want to figure out together which ones matter most?",
  virus: "There's something worth attention on the Virus/Defender side — want me to explain what to do?",
  thermal: "Your system seems to be running hot or throttling — want to dig into why together?",
  security: "There's a gap in your security settings (firewall/UAC) — want to know why it matters?",
  updates: "You have pending updates — want to talk through which ones matter most?",
  startup: "Your boot time looks slow — want to find out together what's slowing it down?",
  crashes: "There's a recurring crash/freeze lately — curious what might be causing it?",
  pagefile: "Your virtual memory setting could use a look — want me to explain why I'm suggesting this?",
  cleanup: "There's a fair amount of space you could reclaim — where do you want to start?",
} as const;
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run src/lib/i18n/__tests__/mascotPrompts.test.ts`
Expected: PASS (2 tests). TypeScript ayrıca `mascotPromptsEn`'de eksik bir
key olursa derleme zamanında hata verir (`Record<MascotCategoryKey, string>`).

- [ ] **Step 5: Commit**

```bash
git add src/lib/i18n/mascotPrompts.tr.ts src/lib/i18n/mascotPrompts.en.ts src/lib/i18n/__tests__/mascotPrompts.test.ts
git commit -m "feat(mascot): per-category static prompt dictionary (TR/EN)"
```

---

## Task 3: `mascotSeenSignatures` ayar alanı (backend + frontend)

**Files:**
- Modify: `src-tauri/src/settings.rs`
- Modify: `src/lib/settings.ts`

- [ ] **Step 1: Rust tarafına alanı ekle**

`src-tauri/src/settings.rs` içinde `Settings` struct'ına (mevcut
`ai_disclosure_ack` alanının hemen altına) ekle:

```rust
    /// Faz: AI maskotu — kategori key → o kategoriye en son gösterilen
    /// maskot bulgu imzası. Aynı imza tekrar görülürse maskot sessiz kalır.
    #[serde(default)]
    pub mascot_seen_signatures: std::collections::HashMap<String, String>,
```

`impl Default for Settings` bloğundaki struct literal'e ekle:

```rust
            mascot_seen_signatures: std::collections::HashMap::new(),
```

- [ ] **Step 2: Derle**

Run: `cd src-tauri && cargo build 2>&1 | tail -20`
Expected: temiz derleme (yeni alan `#[serde(default)]` sayesinde eski
`settings.json` dosyaları da sorunsuz deserialize olur).

- [ ] **Step 3: TS tarafına karşılığını ekle**

`src/lib/settings.ts`'de `AppSettings` interface'ine (`aiDisclosureAck`'ın
altına) ekle:

```typescript
  /** Kategori key -> o kategoriye en son gösterilen maskot bulgu imzası. */
  mascotSeenSignatures: Record<string, string>;
```

`DEFAULT_SETTINGS` literal'ine ekle:

```typescript
  mascotSeenSignatures: {},
```

- [ ] **Step 4: Doğrula**

Run: `npx tsc -b --noEmit`
Expected: hata yok.

- [ ] **Step 5: Commit**

```bash
git add src-tauri/src/settings.rs src/lib/settings.ts
git commit -m "feat(mascot): persist per-category seen-signature in settings"
```

---

## Task 4: `bounce-in` animasyonu

**Files:**
- Modify: `src/index.css`

- [ ] **Step 1: Keyframe + utility token ekle**

`src/index.css`'de `--animate-pulse-glow` satırının hemen altına ekle:

```css
  --animate-bounce-in: bounce-in 0.5s cubic-bezier(0.34, 1.56, 0.64, 1) both;
```

`@keyframes pulse-glow` bloğunun hemen üstüne (veya altına) ekle:

```css
  @keyframes bounce-in {
    0% {
      opacity: 0;
      transform: translateY(14px) scale(0.9);
    }
    60% {
      opacity: 1;
      transform: translateY(-3px) scale(1.03);
    }
    100% {
      opacity: 1;
      transform: translateY(0) scale(1);
    }
  }
```

- [ ] **Step 2: Doğrula**

Run: `npm run build 2>&1 | tail -10`
Expected: Vite build temiz (CSS söz dizimi hatası yok).

- [ ] **Step 3: Commit**

```bash
git add src/index.css
git commit -m "feat(mascot): add bounce-in keyframe animation"
```

---

## Task 5: `AiMascotBubble` bileşeni

**Files:**
- Create: `src/components/AiMascotBubble.tsx`

- [ ] **Step 1: Bileşeni yaz**

```tsx
// src/components/AiMascotBubble.tsx
import { useEffect, useState } from "react";
import { BrandMark } from "./BrandMark";
import { cn } from "@/lib/utils";

interface Props {
  /** Gösterilecek soru metni (zaten locale'e göre çözülmüş). */
  text: string;
  /** "floating": sağ-alt köşede belirir, birkaç saniye sonra kendiliğinden
   *  soluklaşır. "inline": panelin içine gömülü, kalıcı şerit — hiç solmaz. */
  variant: "floating" | "inline";
  /** Tıklanınca (maskota veya balona) — sohbeti o soruyla açar. */
  onOpenChat: () => void;
}

const FLOATING_VISIBLE_MS = 6000;

export function AiMascotBubble({ text, variant, onOpenChat }: Props) {
  const [visible, setVisible] = useState(true);

  // Floating varyant birkaç saniye sonra kendiliğinden solur — App state'ini
  // etkilemez (inline varyant aynı `text` ile panelde göstermeye devam eder).
  useEffect(() => {
    if (variant !== "floating") return;
    setVisible(true);
    const timer = setTimeout(() => setVisible(false), FLOATING_VISIBLE_MS);
    return () => clearTimeout(timer);
  }, [variant, text]);

  if (variant === "floating") {
    return (
      <div
        className={cn(
          "fixed bottom-5 right-5 z-30 flex items-end gap-2.5 max-w-xs",
          "transition-opacity duration-500",
          visible ? "opacity-100 animate-bounce-in" : "opacity-0 pointer-events-none"
        )}
        role="status"
      >
        <button
          type="button"
          onClick={onOpenChat}
          aria-label={text}
          className="shrink-0 w-11 h-11 rounded-xl bg-primary text-primary-foreground flex items-center justify-center shadow-lg animate-pulse-glow"
        >
          <BrandMark size="sm" className="[&_.fill-primary]:fill-current" />
        </button>
        <button
          type="button"
          onClick={onOpenChat}
          className="text-left rounded-lg border border-primary/40 bg-card px-3 py-2.5 text-xs leading-relaxed text-foreground shadow-md hover:border-primary/70 transition-colors"
        >
          {text}
        </button>
      </div>
    );
  }

  return (
    <div className="flex items-start gap-2.5 rounded-lg border border-primary/30 bg-primary-soft/20 px-3.5 py-3 mb-2.5 animate-fade-in">
      <span className="shrink-0 w-8 h-8 rounded-lg bg-primary text-primary-foreground flex items-center justify-center">
        <BrandMark size="sm" className="[&_.fill-primary]:fill-current" />
      </span>
      <button
        type="button"
        onClick={onOpenChat}
        className="text-left text-sm leading-relaxed text-foreground hover:text-primary-strong transition-colors"
      >
        {text}
      </button>
    </div>
  );
}
```

**Not:** `BrandMark`'ın kendi SVG'si `className="fill-primary"`/`"stroke-primary-foreground"`
Tailwind class'larını zaten kullanıyor (bkz. `BrandMark.tsx`) — maskotu bir
buton içine koyduğumuzda arka plan zaten `bg-primary` olduğundan, mark'ın
kare arka planını görünmez kılıp (aynı renk) yalnız EKG çizgisi + buton
arka planı görünsün istiyoruz. `[&_.fill-primary]:fill-current` bunu SVG
`fill`'ini butonun `currentColor`'una (yani `text-primary-foreground`) göre
geçersiz kılmak için var — **Task 6'da gerçek tarayıcıda görsel olarak
doğrula**, class-selector Tailwind v4'te beklendiği gibi çalışmazsa
`BrandMark`'a yeni bir `variant="mono"` prop'u eklemek daha güvenilir bir
alternatiftir (o zaman bu notu güncelle).

- [ ] **Step 2: Derle ve gözden geçir**

Run: `npx tsc -b --noEmit`
Expected: hata yok.

- [ ] **Step 3: Commit**

```bash
git add src/components/AiMascotBubble.tsx
git commit -m "feat(mascot): AiMascotBubble component (floating + inline variants)"
```

---

## Task 6: `AiChatDrawer`'a maskot mesajı enjeksiyonu

**Files:**
- Modify: `src/components/AiChatDrawer.tsx:10-14` (Props), sohbet gövdesi

- [ ] **Step 1: Props'a yeni alan ekle**

`AiChatDrawer.tsx`'deki `Props` interface'ini değiştir:

```typescript
interface Props {
  open: boolean;
  onClose: () => void;
  report: ScanReport | null;
  /** Maskottan gelen, sohbete asistan mesajı olarak eklenecek bekleyen soru.
   *  Eklendikten sonra App.tsx `onPendingMessageConsumed` ile null'a çeker —
   *  aynı mesajın tekrar tekrar eklenmesini önler. */
  pendingMascotMessage?: string | null;
  onPendingMascotMessageConsumed?: () => void;
}
```

Fonksiyon imzasını güncelle:

```typescript
export function AiChatDrawer({
  open,
  onClose,
  report,
  pendingMascotMessage,
  onPendingMascotMessageConsumed,
}: Props) {
```

- [ ] **Step 2: Mesajı ekleyen effect'i ekle**

Mevcut "Ollama durumunu kontrol et" `useEffect`'inin (status kontrolü)
hemen altına yeni bir `useEffect` ekle:

```typescript
  // Maskottan bekleyen bir soru varsa ve sohbet hazırsa, asistan mesajı
  // olarak ekle ve App state'inden temizlenmesini iste (tek seferlik).
  useEffect(() => {
    if (!open || !pendingMascotMessage || status !== "ready") return;
    setMessages((prev) => [...prev, { role: "assistant", content: pendingMascotMessage }]);
    onPendingMascotMessageConsumed?.();
  }, [open, pendingMascotMessage, status, onPendingMascotMessageConsumed]);
```

- [ ] **Step 3: Derle**

Run: `npx tsc -b --noEmit`
Expected: hata yok.

- [ ] **Step 4: Commit**

```bash
git add src/components/AiChatDrawer.tsx
git commit -m "feat(mascot): AiChatDrawer accepts a pending mascot question"
```

---

## Task 7: `CategoryDetail`'e inline banner (variant B)

**Files:**
- Modify: `src/components/CategoryDetail.tsx`

- [ ] **Step 1: Props'a ekle**

`CategoryDetail.tsx`'deki `Props` interface'ine ekle:

```typescript
  /** Bu kategori için maskotun gösterdiği soru — varsa panelin üstünde
   *  gömülü şerit olarak render edilir (bkz. AiMascotBubble variant="inline"). */
  mascotPrompt?: string | null;
  onMascotOpenChat?: () => void;
```

Fonksiyon parametrelerine ekle (`cleanupResult` satırının hemen altına):

```typescript
  mascotPrompt,
  onMascotOpenChat,
```

- [ ] **Step 2: Render'a ekle**

Import ekle (dosyanın en üstüne, diğer component import'larının yanına):

```typescript
import { AiMascotBubble } from "./AiMascotBubble";
```

Başlık bloğunun hemen altına, bulgular scroll container'ından ÖNCE (yani
mevcut satır 150 ile 152 arasına) ekle:

```tsx
      {mascotPrompt && onMascotOpenChat && (
        <AiMascotBubble text={mascotPrompt} variant="inline" onOpenChat={onMascotOpenChat} />
      )}
```

- [ ] **Step 3: Derle**

Run: `npx tsc -b --noEmit`
Expected: hata yok (yeni prop'lar opsiyonel — mevcut çağrı yerleri hâlâ geçerli).

- [ ] **Step 4: Commit**

```bash
git add src/components/CategoryDetail.tsx
git commit -m "feat(mascot): inline mascot banner in category detail panel"
```

---

## Task 8: App.tsx'te tetikleme + floating bubble + drawer bağlama

**Files:**
- Modify: `src/App.tsx`

- [ ] **Step 1: Import'ları ekle**

Dosyanın importlarına ekle:

```typescript
import { AiMascotBubble } from "./components/AiMascotBubble";
import { computeFindingSignature, shouldShowMascot } from "./lib/mascotSignature";
import { mascotPromptsTr } from "./lib/i18n/mascotPrompts.tr";
import { mascotPromptsEn } from "./lib/i18n/mascotPrompts.en";
```

- [ ] **Step 2: State ekle**

`selectedCat` state tanımının (satır 139 civarı) hemen altına ekle:

```typescript
  const [mascotPrompt, setMascotPrompt] = useState<{ categoryKey: string; text: string } | null>(
    null
  );
  const [pendingMascotMessage, setPendingMascotMessage] = useState<string | null>(null);
```

- [ ] **Step 3: Kategori seçim handler'ını yaz**

`findingsForSelectedCat` `useMemo`'sunun (satır ~413-417) hemen altına ekle:

```typescript
  // Maskot: bir kategoriye tıklanınca, o kategori için bulgular değiştiyse
  // (veya ilk kez görülüyorsa) bağlamsal bir soru göster.
  const handleCategorySelect = useCallback(
    (cat: CategoryDef) => {
      setSelectedCat(cat);
      if (cat.key === "cleanup" || !report) {
        setMascotPrompt(null);
        return;
      }
      const related = report.findings.filter((f) => cat.matches.includes(f.category));
      if (related.length === 0) {
        setMascotPrompt(null);
        return;
      }
      const signature = computeFindingSignature(related);
      const seen = settingsSnapshot?.mascotSeenSignatures ?? {};
      if (!shouldShowMascot(cat.key, signature, seen)) {
        setMascotPrompt(null);
        return;
      }
      const dict = locale === "en" ? mascotPromptsEn : mascotPromptsTr;
      const text = dict[cat.key as keyof typeof mascotPromptsTr] ?? dict.cleanup;
      setMascotPrompt({ categoryKey: cat.key, text });
      if (settingsSnapshot) {
        const next = {
          ...settingsSnapshot,
          mascotSeenSignatures: { ...settingsSnapshot.mascotSeenSignatures, [cat.key]: signature },
        };
        saveSettings(next)
          .then(setSettingsSnapshot)
          .catch((e) => console.warn("[mascot] seen-signature save failed:", e));
      }
    },
    [report, settingsSnapshot, locale]
  );

  const handleMascotOpenChat = useCallback(() => {
    if (mascotPrompt) setPendingMascotMessage(mascotPrompt.text);
    setChatOpen(true);
  }, [mascotPrompt]);
```

- [ ] **Step 4: `CategoryGrid`'in `onSelect`'ini değiştir**

`<CategoryGrid report={report} onSelect={setSelectedCat} />` satırını
şuna değiştir:

```tsx
              <CategoryGrid report={report} onSelect={handleCategorySelect} />
```

- [ ] **Step 5: Floating bubble'ı render et**

`<CategoryGrid .../>` satırının hemen altına (aynı "ANA panel" div'i içinde) ekle:

```tsx
              {mascotPrompt && (
                <AiMascotBubble
                  key={`${mascotPrompt.categoryKey}:${mascotPrompt.text}`}
                  text={mascotPrompt.text}
                  variant="floating"
                  onOpenChat={handleMascotOpenChat}
                />
              )}
```

(`key` prop'u kategori/metin değişince bileşenin yeniden mount olup
bounce-in animasyonunu tekrar oynatmasını sağlar.)

- [ ] **Step 6: `CategoryDetail`'e yeni prop'ları geçir**

`<CategoryDetail ... />` çağrısına (satır ~801-816) ekle:

```tsx
                  mascotPrompt={
                    selectedCat && mascotPrompt?.categoryKey === selectedCat.key
                      ? mascotPrompt.text
                      : null
                  }
                  onMascotOpenChat={handleMascotOpenChat}
```

- [ ] **Step 7: `AiChatDrawer`'a yeni prop'ları geçir**

`<AiChatDrawer open={chatOpen} onClose={() => setChatOpen(false)} report={report} />`
satırını şuna değiştir:

```tsx
        <AiChatDrawer
          open={chatOpen}
          onClose={() => setChatOpen(false)}
          report={report}
          pendingMascotMessage={pendingMascotMessage}
          onPendingMascotMessageConsumed={() => setPendingMascotMessage(null)}
        />
```

- [ ] **Step 8: Derle**

Run: `npx tsc -b --noEmit`
Expected: hata yok.

- [ ] **Step 9: `npm run check:all` çalıştır**

Run: `npm run check:all`
Expected: 9/9 yeşil (version + i18n + fmt + clippy + prettier + security
invariants + lib tests + vitest + frontend build).

- [ ] **Step 10: Elle doğrula**

Run: `npm run tauri dev`

1. TARA'ya bas, en az bir kritik/uyarı bulgusu olan bir kategoriye tıkla.
2. Sağ-alt köşede maskotun bounce-in ile belirdiğini, birkaç saniye sonra
   soluklaştığını doğrula.
3. Kategori detay panelinin üstünde aynı sorunun şerit olarak durduğunu
   (solmadan) doğrula.
4. Şeride veya maskota tıkla → sohbet çekmecesi açılmalı, o soru asistan
   mesajı olarak görünmeli (Ollama kuruluysa gerçek yanıt akışı başlar,
   kurulu değilse mevcut "Ollama kurulu değil" ekranı görünür — mesaj
   `status === "ready"` olana kadar bekler, bu senaryoda hiç eklenmez;
   bu kabul edilebilir — kullanıcı önce Ollama'yı kurar).
5. Panelden geri çık, AYNI kategoriye TEKRAR tıkla → maskot bu sefer
   belirmemeli (imza aynı kaldı).
6. Farklı bir kategoriye tıkla → o kategori için maskot belirmeli.

- [ ] **Step 11: Commit**

```bash
git add src/App.tsx
git commit -m "feat(mascot): wire category click -> mascot -> chat handoff in App.tsx"
```

---

## Task 9 (opsiyonel/stretch): Ollama zenginleştirmesi

**Kapsam notu:** Görev 1-8 zaten tam, kendi başına çalışan bir özellik
üretir (spec'in "sabit soru her zaman anında görünür" gereksinimi tam
karşılanıyor). Bu görev YALNIZCA Ollama çalışıyorsa sabit soruyu daha
spesifik hale getiren bonus bir katman — süre kısıtlıysa atlanabilir,
mascot yine de tam işlevsel kalır.

**Risk:** `aiChat`/`onAiToken`/`onAiDone` GLOBAL event'ler (bkz.
`AiChatDrawer.tsx`) — birden fazla eşzamanlı istek stream'leri karıştırır.
Bu yüzden zenginleştirme **yalnızca sohbet çekmecesi KAPALIYKEN** tetiklenir.

**Files:**
- Modify: `src/components/AiMascotBubble.tsx`

- [ ] **Step 1: Zenginleştirme prop'larını ekle**

`Props` interface'ine ekle:

```typescript
  /** Ollama çalışıyorsa (ve sohbet çekmecesi kapalıyken) sabit soruyu
   *  gerçek bulgu verisiyle kişiselleştirir. Verilmezse zenginleştirme
   *  hiç denenmez (örn. sohbet açıkken App.tsx bunu geçmez). */
  enrichWith?: { model: string; findingsSummary: string } | null;
```

- [ ] **Step 2: Zenginleştirme effect'ini ekle**

`AiMascotBubble` fonksiyonunun içine (mevcut `useEffect`'in altına):

```typescript
  const [enrichedText, setEnrichedText] = useState<string | null>(null);

  useEffect(() => {
    setEnrichedText(null);
    if (!enrichWith) return;
    let cancelled = false;
    let acc = "";
    const unlisteners: Array<() => void> = [];
    Promise.all([
      onAiToken((chunk) => {
        acc += chunk;
      }),
      onAiDone(() => {
        if (!cancelled && acc.trim()) setEnrichedText(acc.trim());
      }),
      onAiError(() => {
        /* sessizce yut — sabit soru zaten ekranda, zenginleştirme opsiyonel */
      }),
    ]).then((u) => unlisteners.push(...u));
    void aiChat(enrichWith.model, [
      {
        role: "system",
        content:
          "Kullanıcıya kısa (tek cümle), meraklı bir soru sor. Verdiğim bağlamı kullan ama uydurma ekleme.",
      },
      { role: "user", content: `${text}\n\nBağlam: ${enrichWith.findingsSummary}` },
    ]).catch(() => {
      /* sessizce yut */
    });
    return () => {
      cancelled = true;
      unlisteners.forEach((u) => u());
    };
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [enrichWith?.model, enrichWith?.findingsSummary]);
```

Gerekli import'ları dosyanın üstüne ekle:

```typescript
import { aiChat, onAiDone, onAiError, onAiToken } from "@/lib/api";
```

Render'da `text` yerine `enrichedText ?? text` kullan (hem floating hem
inline dönüşlerinde).

- [ ] **Step 3: App.tsx'ten `enrichWith`'i geç**

`handleCategorySelect` içinde, Ollama modelinin adı elde mevcut değil —
bu görev için `aiListModels()`'i `App.tsx` mount effect'inde bir kez
çağırıp sonucu bir state'te (`const [aiModel, setAiModel] = useState<string | null>(null)`)
tutmak gerekir; `chatOpen === false` olduğu sürece
`enrichWith={aiModel ? { model: aiModel, findingsSummary: ... } : null}`
olarak floating `AiMascotBubble`'a geçir. `findingsSummary`, `related`
bulgularının `title`/`metric` alanlarından kısa bir özet (PII-temiz,
mevcut `buildContext` deseniyle tutarlı).

**Not:** Bu adımın tam kodu, `aiListModels` çağrısının App.tsx'in neresine
oturacağına dair bir tasarım kararı gerektiriyor (mount'ta mı, ilk maskot
tetiklendiğinde mi) — uygulayan mühendis bunu Task 8'in mevcut
`useEffect`'iyle (satır 151-173) tutarlı şekilde eklemeli. Süre kısıtlıysa
bu görevi tamamen atla; Task 1-8 zaten eksiksiz bir özellik teslim eder.

- [ ] **Step 4: Elle doğrula**

Ollama kurulu bir makinede: kategoriye tıkla, birkaç saniye içinde
maskotun sorusunun bulgu detayına göre (örn. sürücü harfi, yüzde) daha
spesifik bir metne dönüştüğünü gözle doğrula. Sohbet açıkken tıklarsan
zenginleştirmenin hiç denenmediğini (network/stream log'unda ikinci bir
istek olmadığını) doğrula.

- [ ] **Step 5: Commit**

```bash
git add src/components/AiMascotBubble.tsx src/App.tsx
git commit -m "feat(mascot): optional Ollama enrichment of the static prompt"
```

---

## Self-Review (bu plan için yapıldı)

1. **Spec kapsaması:** §1 yerleşim → Task 5-8. §2 soru kaynağı → Task 2 (sabit) + Task 9 (Ollama). §3 tekrar kuralı → Task 1+3+8. §4 tıklama davranışı → Task 6+8. §5 animasyon → Task 4. §6 test → Task 1+2 (gerçekçi hale getirildi, bkz. üstteki düzeltme notu). Tüm bölümler kapsanıyor.
2. **Placeholder taraması:** Yok — her adımda tam kod var.
3. **Tip tutarlılığı:** `CategoryDef["key"]` (`string`) ↔ `mascotPromptsTr` key'leri (13 literal) ↔ `Record<string,string>` (`mascotSeenSignatures`) — hepsi düz `string` üzerinden uyumlu, tip çakışması yok. `AiMascotBubble`'ın `onOpenChat: () => void` imzası Task 5/7/8'de tutarlı.
