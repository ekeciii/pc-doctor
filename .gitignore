node_modules
dist
dist-ssr
.vite
*.tsbuildinfo

# Rust / Tauri
src-tauri/target
# Cargo.lock TAKİP EDİLİR (Faz 3) — reproducible build + anlamlı `cargo audit`
# için bir uygulama crate'inde lockfile commit edilmeli (kütüphane değil, bu bir bin/app).
src-tauri/WixTools

# Logs
*.log
npm-debug.log*
yarn-debug.log*

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env
.env
.env.local
